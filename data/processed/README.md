# Datos derivados

`heart_unique.csv` se genera mediante `drop_duplicates()` sobre las 14 columnas de `data/raw/heart.csv`.

Es una versión derivada destinada exclusivamente al análisis reproducible de duplicados y representatividad. No reemplaza al archivo original, que continúa siendo la fuente primaria e inmutable.
